{
  "name": "@devdigest/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Local MCP (stdio) server over the running DevDigest API. A thin HTTP client + protocol adapter: five tools that forward to @devdigest/api on localhost:3001. Owns no data, no Postgres pool, no job runner, no LLM key — the API must already be running.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "inspect": "pnpm dlx @modelcontextprotocol/inspector@2.4.0 tsx src/index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:live": "tsx test/live.manual.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
